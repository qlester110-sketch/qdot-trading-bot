.env
.env.*
!env.template

# Local fallback when Slack credentials are missing (top-level only —
# anchor with leading slash so routines/daily-summary.md isn't matched)
/DAILY-SUMMARY.md

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp
__pycache__/
